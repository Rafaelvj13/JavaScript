let nome = "rafael";

/*console.log:
 O console.log é uma função do objeto console que é usado para
 imprimir mensagens de depuração ou informações no console do 
 navegador ou no ambiente em que o JavaScript
 está sendo executado. É uma maneira útil de exibir valores de 
 variáveis, mensagens de status ou qualquer outra informação 
 relevante durante o desenvolvimento ou depuração do código JavaScript. */
console.log(nome);
console.error(nome);
console.warn(nome);

/*window:
O window é um objeto global no navegador que representa a janela atual 
ou o contexto de execução do JavaScript. Ele fornece acesso a várias 
funcionalidades e propriedades relacionadas à janela do navegador,
 como manipulação do DOM, abertura de janelas pop-up, manipulação de 
 cookies e muito mais. */

//window.alert(nome);
//nome = window.prompt("digite seu nome");
//console.log(nome);
//let confirmacao = window.confirm("confirma essa ação");
//console.log(confirmacao);

alert("Olá mundo");
confirm("confirma essa ação");
prompt("digite seu nome");
